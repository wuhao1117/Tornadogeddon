using UnityEngine;
using System.Collections;

public class DialoguePicScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Vector3 positionVec = new Vector3(0.01f,0.82f,0.0f);
		
		gameObject.transform.position = positionVec;
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
